services:
  web:
    build: .
    container_name: flask_crud_backend
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - .:/app
      - sqlite_data:/app/instance

volumes:
  sqlite_data:
